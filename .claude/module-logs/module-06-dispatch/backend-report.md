# Module 06 — Dispatch Backend Report

## Summary
All backend tasks for Module 06 (Live Dispatch & Exception Console) completed successfully.

---

## Files Created

| File | What was done |
|---|---|
| `backend/app/models/dispatch.py` | New models: `DispatchException` (dispatch_exceptions table) and `SurgeOverride` (surge_overrides table) with all required fields, FKs, TimestampMixin |
| `backend/app/schemas/dispatch.py` | All Pydantic v2 request/response schemas matching api-contract.md exactly. Uses `from __future__ import annotations` throughout. |
| `backend/app/services/dispatch_service.py` | Full service layer: get_queue, get_queue_stats, get_eligible_drivers, assign_driver, expand_radius, get_exceptions, resolve_exception, get_supply, create_surge_override, get_surge_overrides. Includes haversine_km helper. |
| `backend/app/api/v1/endpoints/dispatch.py` | FastAPI router with all 10 endpoints, auth guard on every endpoint, follows catalog.py pattern exactly. |
| `backend/alembic/versions/9312b379c6e6_add_module_06_dispatch.py` | Autogenerated migration: adds new tables + columns to existing tables. |
| `backend/alembic/versions/523b842a6a2a_merge_heads_before_dispatch.py` | Merge migration needed to resolve pre-existing multiple heads (module-05 stamp). |

---

## Files Modified

| File | What was changed |
|---|---|
| `backend/app/models/booking.py` | Added 4 columns to `RoadBooking`: `dispatch_attempts`, `current_radius_km`, `zone_id`, `zone_name` |
| `backend/app/models/driver.py` | Added 2 columns to `Driver`: `current_lat`, `current_lng` |
| `backend/app/models/__init__.py` | Imported `DispatchException`, `SurgeOverride` from dispatch model |
| `backend/app/api/v1/router.py` | Registered dispatch router with prefix `/dispatch` and tag `Dispatch` |

---

## Endpoints Registered

| Method | Path | Description |
|---|---|---|
| GET | /api/v1/dispatch/queue | Live request queue |
| GET | /api/v1/dispatch/queue/stats | Aggregate stats |
| GET | /api/v1/dispatch/requests/{booking_id}/eligible-drivers | Ranked eligible drivers |
| POST | /api/v1/dispatch/requests/{booking_id}/assign | Manual driver assignment |
| POST | /api/v1/dispatch/requests/{booking_id}/expand-radius | Expand search radius |
| GET | /api/v1/dispatch/exceptions | Active exceptions |
| POST | /api/v1/dispatch/exceptions/{exception_id}/resolve | Resolve exception |
| GET | /api/v1/dispatch/supply | Supply/demand per zone |
| POST | /api/v1/dispatch/surge/override | Create surge override |
| GET | /api/v1/dispatch/surge/overrides | List surge overrides |

---

## Notes
- Migration file created but NOT applied (per instructions). Run `alembic upgrade head` to apply.
- Haversine formula used for driver proximity calculations.
- Surge cap enforced at 2.0× multiplier.
- `from __future__ import annotations` on all new files using `X | None` union syntax.
