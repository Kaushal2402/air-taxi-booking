import 'package:flutter/material.dart';

// SCREEN 2.1 — Home Dashboard
// Full spec: .claude/module-logs/mobile-customer-02-home/IMPLEMENTATION_BRIEF.md
// Implement: forest hero header with greeting row + notification bell + avatar,
// quick-book card overlapping hero (marginTop: -40), service type filter chips,
// upcoming trip card (dark forest card with route arc), popular routes
// horizontal scroll, bottom navigation bar (4 tabs).

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('HomeScreen — TODO')),
    );
  }
}
