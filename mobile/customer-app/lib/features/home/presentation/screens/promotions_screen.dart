import 'package:flutter/material.dart';

// SCREEN 2.5 — Promotions & Offers
// Full spec: .claude/module-logs/mobile-customer-02-home/IMPLEMENTATION_BRIEF.md
// Implement: hero promo banner (forest gradient, "20% off." serif, code pill +
// copy-to-clipboard CTA), coupon list cards (accent strip + percent icon +
// label + code + expiry), referral card (mint bg), bottom navigation bar.

class PromotionsScreen extends StatelessWidget {
  const PromotionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('PromotionsScreen — TODO')),
    );
  }
}
