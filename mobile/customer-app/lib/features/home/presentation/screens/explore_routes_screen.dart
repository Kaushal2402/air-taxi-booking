import 'package:flutter/material.dart';

// SCREEN 2.3 — Explore Routes
// Full spec: .claude/module-logs/mobile-customer-02-home/IMPLEMENTATION_BRIEF.md
// Implement: serif headline "Explore routes.", pill search bar,
// horizontal city filter chips (All/Mumbai/Delhi/Bangalore/Goa),
// route list cards (type icon, city → city, availability badge,
// duration, IATA code, price), bottom navigation bar (active: explore).

class ExploreRoutesScreen extends StatelessWidget {
  const ExploreRoutesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ExploreRoutesScreen — TODO')),
    );
  }
}
