import 'package:flutter/material.dart';

// SCREEN 2.2 — All Services
// Full spec: .claude/module-logs/mobile-customer-02-home/IMPLEMENTATION_BRIEF.md
// Implement: list of service type cards (dark gradient for top 2, light surface
// for bottom 2), icon + label + sub + price, loyalty/miles card at bottom,
// bottom navigation bar.

class AllServicesScreen extends StatelessWidget {
  const AllServicesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('AllServicesScreen — TODO')),
    );
  }
}
