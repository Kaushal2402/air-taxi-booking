import 'package:flutter/material.dart';

// SCREEN 1.2 — Welcome / Onboarding
// Full spec: .claude/module-logs/mobile-customer-01-auth/IMPLEMENTATION_BRIEF.md
// Implement: forest hero with SVG cityscape + route arcs, service chips,
// content card overlapping hero (-24px marginTop), slide dots,
// serif headline "Book your sky. Any time.", CTA buttons.

class WelcomeScreen extends StatelessWidget {
  const WelcomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('WelcomeScreen — TODO')),
    );
  }
}
