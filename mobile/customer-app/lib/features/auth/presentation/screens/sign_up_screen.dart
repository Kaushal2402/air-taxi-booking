import 'package:flutter/material.dart';

// SCREEN 1.5 — Sign Up
// Full spec: .claude/module-logs/mobile-customer-01-auth/IMPLEMENTATION_BRIEF.md
// Implement: forest header with SVG rings, name/email/phone/password fields,
// StrengthBar widget, terms checkbox, primary CTA "Create account",
// sign-in footer link.

class SignUpScreen extends StatelessWidget {
  const SignUpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SignUpScreen — TODO')),
    );
  }
}
