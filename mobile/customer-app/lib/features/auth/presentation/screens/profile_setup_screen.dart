import 'package:flutter/material.dart';

// SCREEN 1.6 — Profile Setup (Step 3 of 3)
// Full spec: .claude/module-logs/mobile-customer-01-auth/IMPLEMENTATION_BRIEF.md
// Implement: forest header with 3-step tracker, avatar upload circle with
// camera badge, display name + home city fields, notifications toggle,
// "Complete setup" + "Skip" CTAs.

class ProfileSetupScreen extends StatelessWidget {
  const ProfileSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ProfileSetupScreen — TODO')),
    );
  }
}
